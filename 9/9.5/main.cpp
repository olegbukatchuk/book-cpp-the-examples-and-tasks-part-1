#include <iostream>
#include <cstdlib>

using namespace std;

void findA(double* a, double* x, double* y, int n) {
    double q;
    a[0]=y[0];

    for (int k=1;k<n;k++) {
        a[k]=y[k];
        q=1;

        for ()
    }
}